services:
  db:
    image: postgres:16-alpine
    restart: unless-stopped
    environment:
      POSTGRES_USER: commons
      POSTGRES_PASSWORD: commons
      POSTGRES_DB: commons
    ports:
      - "5432:5432"
    volumes:
      - commons_pg_data:/var/lib/postgresql/data

volumes:
  commons_pg_data:
